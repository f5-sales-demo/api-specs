{
  "name": "@f5-sales-demo/api-specs",
  "version": "0.0.0",
  "private": true,
  "description": "Node tooling for the F5 XC OpenAPI spec pipeline. Spectral is the OAS3 lint gate; scripts/spectral_lint.py resolves it from PATH, which `npm ci` populates via node_modules/.bin.",
  "license": "Apache-2.0",
  "devDependencies": {
    "@stoplight/spectral-cli": "6.16.2"
  }
}
